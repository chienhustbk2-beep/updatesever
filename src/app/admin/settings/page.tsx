'use client';import { useState, useEffect, startTransition } from 'react';import { Save, Loader2, Check, AlertCircle, Settings, CreditCard, Mail, Shield, Globe, MessageCircle, MessageSquare, ThumbsUp, Banknote, Copy, CheckCircle2 } from 'lucide-react';
import StickyActionBar from '@/components/ui/StickyActionBar';
interface SettingsData { siteName: string; siteLogo: string; contactEmail: string; contactPhone: string; footerText: string; bankName: string; bankAccount: string; bankAccountName: string; momoAccessKey: string; momoSecretKey: string; zalopayAppId: string; zalopayKey1: string; smtpHost: string; smtpPort: string; smtpUser: string; smtpPassword: string; smtpFromEmail: string; contactZalo: string; contactTelegram: string; contactFacebook: string }
const defaultSettings: SettingsData = {  siteName: '',  siteLogo: '',  contactEmail: '',  contactPhone: '',  footerText: '',  bankName: '',  bankAccount: '',  bankAccountName: '',  momoAccessKey: '',  momoSecretKey: '',  zalopayAppId: '',  zalopayKey1: '',  smtpHost: '',  smtpPort: '587',  smtpUser: '',  smtpPassword: '',  smtpFromEmail: '',  contactZalo: '',  contactTelegram: '',  contactFacebook: ''}
export default function AdminSettingsPage() {  const [settings, setSettings] = useState<SettingsData>(defaultSettings);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [activeTab, setActiveTab] = useState('general');
  const [message, setMessage] = useState<{ type: 'success' | 'error'; text: string } | null>(null);
  const [copied, setCopied] = useState(false);
  const fetchSettings = async () => {    try {      const res = await fetch('/api/admin/settings');
  const data = await res.json();
  if (res.ok) {        setSettings({ ...defaultSettings, ...data.settings })      }
} catch (err) {      console.error('Failed to fetch settings:', err)    } finally {      setLoading(false) } }; useEffect(() => {    startTransition(() => {      fetchSettings()    })  }, []);
  const handleSave = async () => {    setSaving(true);setMessage(null);
try {      const res = await fetch('/api/admin/settings', {        method: 'POST',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ settings }),      })
  const data = await res.json();
  if (res.ok) {        setMessage({ type: 'success', text: 'Lưu cấu hình thành công!' })      } else {        setMessage({ type: 'error', text: data.error })      }
} catch {      setMessage({ type: 'error', text: 'Có lỗi xảy ra' })    } finally {      setSaving(false)    }    setTimeout(() => setMessage(null), 3000)  }
const tabs = [    { id: 'general', label: 'Chung', icon: Globe },    { id: 'payment', label: 'Thanh toán', icon: CreditCard },    { id: 'email', label: 'Email', icon: Mail },    { id: 'contact', label: 'Liên hệ', icon: MessageCircle },    { id: 'sepay', label: 'SePay NH', icon: Banknote },    { id: 'security', label: 'Bảo mật', icon: Shield },  ];if (loading) {    return (      <div className="flex min-h-[60vh] items-center justify-center">        <div className="text-center">          <div className="mx-auto h-8 w-8 animate-spin rounded-full border-2 border-[var(--primary)] border-t-transparent" />          <p className="mt-4 text-sm text-muted">Đang tải cấu hình...</p>        </div>      </div>    )  }
return (    <div className="min-h-screen px-4 py-8 sm:px-6 lg:px-8 text-main">      {/* Header */}      <div className="mb-8">        <h1 className="text-3xl font-extrabold gradient-heading">          Cấu hình Hệ thống        </h1>        <p className="mt-2 text-sm text-muted">          Quản lý các thiết lập chung của hệ thống        </p>      </div>      {/* Message */}      {message && (        <div className={`mb-6 rounded-xl p-4 ${message.type === 'success' ? 'bg-[var(--success)]/10 text-[var(--success)] border border-[var(--success)]/20' : 'bg-[var(--danger)]/10 text-[var(--danger)] border border-[var(--danger)]/20'}`}>          {message.text}        </div>      )}      {/* Tabs */}      <div className="mb-6 flex gap-2 border-b border-divider">        {tabs.map((tab) => {          const Icon = tab.icon; return (            <button              key={tab.id}              onClick={() => setActiveTab(tab.id)}              className={`flex items-center gap-2 px-4 py-3 text-sm font-medium border-b-2 transition ${                activeTab === tab.id                  ? 'border-[var(--primary)] text-[var(--primary)]'                  : 'border-transparent text-muted hover:text-muted'              }`}            >              <Icon className="h-4 w-4" />              {tab.label}            </button>          )        })}      </div>      {/* Tab Content */}      <div className="rounded-2xl border border-divider bg-card p-6">        {activeTab === 'general' && (          <div className="space-y-4">            <h2 className="text-lg font-bold text-main mb-4">Cấu hình chung</h2>            <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">              <div>                <label className="block text-sm font-medium text-muted mb-1">Tên website</label>                <input                  value={settings.siteName}                  onChange={(e) => setSettings({ ...settings, siteName: e.target.value })}                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>              <div>                <label className="block text-sm font-medium text-muted mb-1">URL Logo</label>                <input                  value={settings.siteLogo}                  onChange={(e) => setSettings({ ...settings, siteLogo: e.target.value })}                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>              <div>                <label className="block text-sm font-medium text-muted mb-1">Email liên hệ</label>                <input                  value={settings.contactEmail}                  onChange={(e) => setSettings({ ...settings, contactEmail: e.target.value })}                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>              <div>                <label className="block text-sm font-medium text-muted mb-1">Số điện thoại</label>                <input                  value={settings.contactPhone}                  onChange={(e) => setSettings({ ...settings, contactPhone: e.target.value })}                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>              <div className="sm:col-span-2">                <label className="block text-sm font-medium text-muted mb-1">Footer text</label>                <input                  value={settings.footerText}                  onChange={(e) => setSettings({ ...settings, footerText: e.target.value })}                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>            </div>          </div>        )}        {activeTab === 'payment' && (          <div className="space-y-6">            <h2 className="text-lg font-bold text-main mb-4">Cấu hình thanh toán</h2>            {/* Bank Transfer */}            <div className="rounded-xl bg-card border border-divider p-4">              <h3 className="text-sm font-semibold text-[var(--primary)] mb-3">Thông tin chuyển khoản</h3>              <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">                <div>                  <label className="block text-sm font-medium text-muted mb-1">Tên ngân hàng</label>                  <input                    value={settings.bankName}                    onChange={(e) => setSettings({ ...settings, bankName: e.target.value })}                    className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                  />                </div>                <div>                  <label className="block text-sm font-medium text-muted mb-1">Số tài khoản</label>                  <input                    value={settings.bankAccount}                    onChange={(e) => setSettings({ ...settings, bankAccount: e.target.value })}                    className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                  />                </div>                <div className="sm:col-span-2">                  <label className="block text-sm font-medium text-muted mb-1">Tên chủ tài khoản</label>                  <input                    value={settings.bankAccountName}                    onChange={(e) => setSettings({ ...settings, bankAccountName: e.target.value })}                    className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                  />                </div>              </div>            </div>            {/* MoMo */}            <div className="rounded-xl bg-card border border-divider p-4">              <h3 className="text-sm font-semibold text-[var(--primary)] mb-3">Ví MoMo</h3>              <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">                <div>                  <label className="block text-sm font-medium text-muted mb-1">Access Key</label>                  <input                    value={settings.momoAccessKey}                    onChange={(e) => setSettings({ ...settings, momoAccessKey: e.target.value })}
type="password"                    className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                  />                </div>                <div>                  <label className="block text-sm font-medium text-muted mb-1">Secret Key</label>                  <input                    value={settings.momoSecretKey}                    onChange={(e) => setSettings({ ...settings, momoSecretKey: e.target.value })}
type="password"                    className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                  />                </div>              </div>            </div>            {/* ZaloPay */}            <div className="rounded-xl bg-card border border-divider p-4">              <h3 className="text-sm font-semibold text-[var(--primary)] mb-3">ZaloPay</h3>              <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">                <div>                  <label className="block text-sm font-medium text-muted mb-1">App ID</label>                  <input                    value={settings.zalopayAppId}                    onChange={(e) => setSettings({ ...settings, zalopayAppId: e.target.value })}                    className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                  />                </div>                <div>                  <label className="block text-sm font-medium text-muted mb-1">Key 1</label>                  <input                    value={settings.zalopayKey1}                    onChange={(e) => setSettings({ ...settings, zalopayKey1: e.target.value })}
type="password"                    className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                  />                </div>              </div>            </div>          </div>        )}        {activeTab === 'email' && (          <div className="space-y-4">            <h2 className="text-lg font-bold text-main mb-4">Cấu hình Email (SMTP)</h2>            <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">              <div>                <label className="block text-sm font-medium text-muted mb-1">SMTP Host</label>                <input                  value={settings.smtpHost}                  onChange={(e) => setSettings({ ...settings, smtpHost: e.target.value })}                  placeholder="smtp.gmail.com"                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>              <div>                <label className="block text-sm font-medium text-muted mb-1">SMTP Port</label>                <input                  value={settings.smtpPort}                  onChange={(e) => setSettings({ ...settings, smtpPort: e.target.value })}                  placeholder="587"                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>              <div>                <label className="block text-sm font-medium text-muted mb-1">SMTP User</label>                <input                  value={settings.smtpUser}                  onChange={(e) => setSettings({ ...settings, smtpUser: e.target.value })}                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>              <div>                <label className="block text-sm font-medium text-muted mb-1">SMTP Password</label>                <input                  value={settings.smtpPassword}                  onChange={(e) => setSettings({ ...settings, smtpPassword: e.target.value })}
type="password"                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>              <div className="sm:col-span-2">                <label className="block text-sm font-medium text-muted mb-1">From Email</label>                <input                  value={settings.smtpFromEmail}                  onChange={(e) => setSettings({ ...settings, smtpFromEmail: e.target.value })}                  placeholder="noreply@yourdomain.com"                  className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main focus:border-[var(--primary)] focus:outline-none focus:ring-1 focus:ring-[var(--primary)]"                />              </div>            </div>          </div>        )}        {activeTab === 'contact' && (          <div className="space-y-4">            <h2 className="text-lg font-bold text-main mb-4">Cấu hình Liên hệ</h2>            <p className="text-sm text-muted mb-4">Cài đặt các kênh liên hệ hiển thị trên website. Icons sẽ xuất hiện ở góc dưới bên phải màn hình.</p>            <div className="rounded-xl bg-card border border-divider p-5">              <div className="grid grid-cols-1 gap-4 md:grid-cols-2">                <div>                  <label className="block text-sm font-medium text-muted mb-1.5">Zalo (URL)</label>                  <div className="flex items-center gap-2">                    <MessageCircle className="h-4 w-4 text-[var(--primary)] shrink-0" />                    <input                      value={settings.contactZalo}                      onChange={(e) => setSettings({ ...settings, contactZalo: e.target.value })}                      className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main placeholder-muted focus:border-[var(--primary)] focus:outline-none"                      placeholder="https://zalo.me/0123456789"                    />                  </div>                </div>                <div>                  <label className="block text-sm font-medium text-muted mb-1.5">Telegram (URL)</label>                  <div className="flex items-center gap-2">                    <MessageSquare className="h-4 w-4 text-[var(--primary)] shrink-0" />                    <input                      value={settings.contactTelegram}                      onChange={(e) => setSettings({ ...settings, contactTelegram: e.target.value })}                      className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main placeholder-muted focus:border-[var(--primary)] focus:outline-none"                      placeholder="https://t.me/username"                    />                  </div>                </div>                <div>                  <label className="block text-sm font-medium text-muted mb-1.5">Facebook (URL)</label>                  <div className="flex items-center gap-2">                    <ThumbsUp className="h-4 w-4 text-[var(--primary)] shrink-0" />                    <input                      value={settings.contactFacebook}                      onChange={(e) => setSettings({ ...settings, contactFacebook: e.target.value })}                      className="w-full rounded-lg bg-main border border-divider px-3 py-2 text-sm text-main placeholder-muted focus:border-[var(--primary)] focus:outline-none"                      placeholder="https://facebook.com/username"                    />                  </div>                </div>              </div>            </div>          </div>        )}        {activeTab === 'sepay' && (          <div className="space-y-4">            <h2 className="text-lg font-bold text-main mb-4">Tự động nạp tiền qua SePay</h2>            <div className="rounded-xl bg-card border border-divider p-5">              <h3 className="text-sm font-semibold text-[var(--primary)] mb-3 flex items-center gap-2">                <Banknote className="h-4 w-4" />                Webhook URL              </h3>              <p className="text-sm text-muted mb-3">                Cấu hình webhook này trong <strong>SePay</strong> để tự động duyệt nạp tiền khi khách hàng chuyển khoản.                Giao thức: parse nội dung chuyển khoản chứa <code className="bg-main px-1.5 py-0.5 rounded text-xs font-mono text-[var(--primary)]">NAP [userId]</code>              </p>              <div className="mt-2 relative pt-[2px]">                <div className="flex items-center gap-2 rounded-lg bg-main border border-default p-3">                  <code className="text-sm text-main font-mono break-all flex-1">                    {typeof window !== 'undefined' ? `${window.location.origin}/api/webhook/sepay` : ''}                  </code>                  <button                    onClick={() => {                      if (typeof window !== 'undefined') {                        navigator.clipboard.writeText(`${window.location.origin}/api/webhook/sepay`)                          .then(() => {                            setCopied(true);setTimeout(() => setCopied(false), 2000)                          })                          .catch(() => {})                      }                    }}                    className="shrink-0 flex items-center gap-1 rounded-lg bg-primary/10 px-3 py-1.5 text-xs font-medium text-[var(--primary)] hover:bg-primary/20 transition"                  >                    <Copy className="h-3 w-3" />                    {copied ? 'Đã copy' : 'Copy'}                  </button>                </div>              </div>            </div>            <div className="rounded-xl bg-card border border-divider p-5">              <h3 className="text-sm font-semibold text-[var(--primary)] mb-3 flex items-center gap-2">                <CheckCircle2 className="h-4 w-4" />                Cách hoạt động              </h3>              <ul className="space-y-2 text-sm text-muted">                <li className="flex items-start gap-2">                  <span className="mt-1 h-1.5 w-1.5 shrink-0 rounded-full bg-[var(--primary)]" />                  Khách hàng chuyển khoản với nội dung <code className="bg-main px-1 rounded text-xs font-mono">NAP [user_id]</code>                </li>                <li className="flex items-start gap-2">                  <span className="mt-1 h-1.5 w-1.5 shrink-0 rounded-full bg-[var(--primary)]" />                  SePay gửi webhook đến server                </li>                <li className="flex items-start gap-2">                  <span className="mt-1 h-1.5 w-1.5 shrink-0 rounded-full bg-[var(--primary)]" />                  Hệ thống ghi nhận giao dịch + cộng tiền tự động                </li>                <li className="flex items-start gap-2">                  <span className="mt-1 h-1.5 w-1.5 shrink-0 rounded-full bg-[var(--primary)]" />                  Admin kiểm tra lịch sử trong <strong>Giao dịch</strong>                </li>              </ul>            </div>            <div className="rounded-xl bg-amber-50 dark:bg-amber-500/10 border border-amber-200 dark:border-amber-500/20 p-4">              <div className="flex items-start gap-3">                <AlertCircle className="h-5 w-5 text-amber-600 dark:text-amber-400 shrink-0 mt-0.5" />                <div>                  <p className="text-sm font-medium text-amber-800 dark:text-amber-300">Lưu ý</p>                  <p className="text-xs text-amber-700 dark:text-amber-400 mt-1">                    Các giao dịch từ SePay tự động được duyệt. Admin có thể duyệt thủ công các giao dịch PENDING qua trang <strong>Giao dịch</strong> trong admin.                  </p>                </div>              </div>            </div>          </div>        )}        {activeTab === 'security' && (          <div className="space-y-4">            <h2 className="text-lg font-bold text-main mb-4">Bảo mật & Phân quyền</h2>            <div className="rounded-xl bg-card border border-divider p-4">              <h3 className="text-sm font-semibold text-[var(--primary)] mb-3">Phân quyền vai trò</h3>              <div className="space-y-3">                <div className="flex items-center justify-between rounded-lg bg-card p-3">                  <div>                    <p className="text-sm font-medium text-main">Quản trị viên (Admin)</p>                    <p className="text-xs text-muted">Toàn quyền truy cập</p>                  </div>                  <span className="px-2 py-1 rounded-full text-xs font-medium bg-[var(--danger)]/10 text-[var(--danger)]">Full Access</span>                </div>                <div className="flex items-center justify-between rounded-lg bg-card p-3">                  <div>                    <p className="text-sm font-medium text-main">Nhân viên (Staff)</p>                    <p className="text-xs text-muted">Quản lý đơn hàng, sản phẩm. Không xem cấu hình hệ thống.</p>                  </div>                  <span className="px-2 py-1 rounded-full text-xs font-medium bg-[var(--primary)]/10 text-[var(--primary)]">Limited</span>                </div>                <div className="flex items-center justify-between rounded-lg bg-card p-3">                  <div>                    <p className="text-sm font-medium text-main">Khách hàng (Customer)</p>                    <p className="text-xs text-muted">Chỉ truy cập trang khách hàng</p>                  </div>                  <span className="px-2 py-1 rounded-full text-xs font-medium bg-[var(--success)]/10 text-[var(--success)]">Basic</span>                </div>              </div>            </div>            <div className="rounded-xl bg-card border border-divider p-4">              <h3 className="text-sm font-semibold text-[var(--primary)] mb-3">Nhật ký hoạt động</h3>              <p className="text-sm text-muted">                Tất cả hành động quan trọng của quản trị viên đều được ghi log: sửa sản phẩm, cập nhật đơn hàng, thay đổi cấu hình...              </p>              <a href="/admin/audit-logs" className="mt-3 inline-flex items-center gap-2 text-sm text-[var(--primary)] hover:text-[var(--primary)] transition">                Xem nhật ký hoạt động →              </a>            </div>          </div>        )}        {/* Save Button */}        <div className="mt-6 pt-6 border-t border-divider flex justify-end">          <button            onClick={handleSave}            disabled={saving}            className="flex items-center gap-2 rounded-lg bg-primary px-6 py-2.5 text-sm font-semibold text-primary-content transition hover:bg-primary disabled:opacity-50"          >            {saving ? (              <>                <Loader2 className="h-4 w-4 animate-spin" />                Đang lưu...              </>            ) : (              <>                <Save className="h-4 w-4" />                Lưu cấu hình              </>            )}          </button>        </div>      </div>    
      <StickyActionBar onSave={handleSave} saving={saving} />
    </div>  )}