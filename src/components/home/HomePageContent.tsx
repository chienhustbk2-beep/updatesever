'use client';import { useUIElements } from '@/components/UIElementsProvider';import { Sparkles, Truck, ArrowRight, ChevronRight } from 'lucide-react';import Link from 'next/link';import { ReactNode } from 'react';
const features = [  { icon: 'Zap', titleKey: 'home.feature_1_title', descKey: 'home.feature_1_desc', color: 'text-[var(--warning)]', bgColor: 'bg-[var(--warning)]/10' },  { icon: 'Shield', titleKey: 'home.feature_2_title', descKey: 'home.feature_2_desc', color: 'text-[var(--success)]', bgColor: 'bg-[var(--success)]/10' },  { icon: 'Headphones', titleKey: 'home.feature_3_title', descKey: 'home.feature_3_desc', color: 'text-[var(--primary)]', bgColor: 'bg-[var(--primary)]/10' },  { icon: 'RefreshCw', titleKey: 'home.feature_4_title', descKey: 'home.feature_4_desc', color: 'text-[var(--primary)]', bgColor: 'bg-[var(--primary)]/10' },];
const iconMap: Record<string, React.ComponentType<{ className?: string }>> = {}
import { Zap, Shield, Headphones, RefreshCw } from 'lucide-react';
iconMap.Zap = Zap;
iconMap.Shield = Shield;
iconMap.Headphones = Headphones;
iconMap.RefreshCw = RefreshCw;
export function FeaturedSection({ children }: { children: ReactNode }) {  const { t, isVisible } = useUIElements();if (!isVisible('features_section') && !isVisible('products_section')) return null;return (    <section className="section-light">      <div className="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8">      {isVisible('features_section') && <div className="mb-12 flex items-center justify-between">        <div>          <div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-widest text-divine-blue text-neon-glow-sm">            <Sparkles className="h-3.5 w-3.5" />            {t('home.new_products')}          </div>          <h2 className="mt-2 text-2xl font-bold text-[var(--foreground)]">{t('home.new_products')}</h2>          <p className="mt-1 text-sm text-muted">            {t('home.new_products_desc')}          </p>        </div>        <Link          href="/products"          className="hidden items-center gap-1 rounded-xl border border-divider card-gradient px-5 py-2.5 text-sm font-medium text-muted transition-all hover:border-[var(--primary)]/40 hover:text-[var(--primary)] sm:flex"        >          {t('home.view_store')} <ChevronRight className="h-4 w-4" />        </Link>      </div>}      {isVisible('products_section') && <>{children}      <div className="mt-12 text-center">        <Link          href="/products"              className="inline-flex items-center gap-2 rounded-xl bg-gradient-to-r from-blue-500 to-indigo-600 px-10 py-4 text-sm font-bold !text-white hover:from-blue-600 hover:to-indigo-700 hover:shadow-lg transition-all"        >          {t('home.view_all')}          <ArrowRight className="h-4 w-4" />        </Link>      </div></>}      </div>    </section>  )}
export function WhyChooseUs() {  const { t, settings, isVisible } = useUIElements();
  const siteName = settings.siteName || "";if (!isVisible('why_choose_us')) return null;return (    <section className="section-alt">      <div className="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8">        <div className="mb-12 text-center">          <div className="mb-2 flex items-center justify-center gap-2 text-xs font-semibold uppercase tracking-widest text-divine-blue text-neon-glow-sm">            <Sparkles className="h-3.5 w-3.5" />            {t('home.why_choose_us')}          </div>          <h2 className="text-2xl font-bold text-[var(--foreground)]">{t('home.why_choose_us_title', { siteName })}</h2>          <p className="mx-auto mt-2 max-w-2xl text-sm text-muted">            {t('home.why_choose_us_desc')}          </p>        </div>        <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">          {features.map((feature) => {            const Icon = iconMap[feature.icon];return (              <div                key={feature.titleKey}                className="group rounded-2xl p-6 text-center transition-all duration-300 shadow-sm hover:-translate-y-2 hover:shadow-xl"              >                <div className={`mx-auto mb-4 flex h-14 w-14 items-center justify-center rounded-full ${feature.bgColor}`}>                  <Icon className={`h-7 w-7 ${feature.color}`} />                </div>                <h3 className="text-base font-semibold text-[var(--foreground)]">{t(feature.titleKey)}</h3>                <p className="mt-2 text-sm leading-relaxed text-muted">{t(feature.descKey)}</p>              </div>            )          })}        </div>      </div>    </section>  )}
export function TrustBadges() {  const { t, settings, isVisible } = useUIElements();
  const siteName = settings.siteName || "";if (!isVisible('trust_badges')) return null;
  const ctaEnabled = settings['homepage_cta_enabled'] !== 'false';
  const trustEnabled = settings['homepage_trust_enabled'] !== 'false';
  if (!ctaEnabled && !trustEnabled) return null;
  const ctaBadge = settings['homepage_cta_badge'] || t('home.cta_badge');
  const ctaTitle = settings['homepage_cta_title'] || t('home.cta_title', { siteName });
  const ctaDesc = settings['homepage_cta_desc'] || t('home.cta_desc');
  const ctaRegister = settings['homepage_cta_register'] || t('home.cta_register');
  const ctaExplore = settings['homepage_cta_explore'] || t('home.cta_explore');
  const trustItems = [    { value: settings['homepage_trust_1_value'] || t('home.trust_1_value'), label: settings['homepage_trust_1_label'] || t('home.trust_1_label') },    { value: settings['homepage_trust_2_value'] || t('home.trust_2_value'), label: settings['homepage_trust_2_label'] || t('home.trust_2_label') },    { value: settings['homepage_trust_3_value'] || t('home.trust_3_value'), label: settings['homepage_trust_3_label'] || t('home.trust_3_label') },    { value: settings['homepage_trust_4_value'] || t('home.trust_4_value'), label: settings['homepage_trust_4_label'] || t('home.trust_4_label') },  ];return (    <section className="relative overflow-hidden section-light">      <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_top_right,_var(--tw-gradient-stops))] from-[var(--accent-hover)]/10 via-transparent to-transparent" />      <div className="absolute -left-20 -top-20 h-60 w-60 rounded-full bg-[var(--accent-color)]/10 blur-3xl" />      <div className="absolute -bottom-20 -right-20 h-60 w-60 rounded-full bg-[var(--accent-color)]/10 blur-3xl" />      <div className="relative mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">        {ctaEnabled && <div className="flex flex-col items-center text-center">            <div className="mb-4 inline-flex items-center gap-2 rounded-full border border-divider px-5 py-2 text-sm font-semibold text-muted backdrop-blur">            <Truck className="h-3.5 w-3.5 text-[var(--success)]" />            {ctaBadge}          </div>          <h2 className="text-3xl font-extrabold text-[var(--foreground)] sm:text-4xl">            {ctaTitle.split(siteName).map((part, i, arr) => (              i < arr.length - 1 ? (                <span key={i}>{part}<span className="bg-gradient-to-r from-[var(--accent-color)] to-[var(--accent-hover)] bg-clip-text text-transparent">{siteName}</span></span>              ) : (                <span key={i}>{part}</span>              )            ))}          </h2>          <p className="mt-3 max-w-xl text-sm text-muted">            {ctaDesc}          </p>          <div className="mt-8 flex flex-wrap items-center justify-center gap-4">            <Link              href="/register"          className="inline-flex items-center gap-2 rounded-xl bg-gradient-to-r from-blue-500 to-indigo-600 px-8 py-3.5 text-sm font-bold text-white shadow-lg hover:from-blue-600 hover:to-indigo-700 hover:shadow-xl transition-all"            >              {ctaRegister}              <ArrowRight className="h-4 w-4" />            </Link>            <Link              href="/products"              className="inline-flex items-center gap-2 rounded-xl border border-divider bg-white/5 px-8 py-3.5 text-sm font-medium text-muted backdrop-blur transition-all hover:border-divider hover:bg-white/10 hover:text-[var(--foreground)]"            >              {ctaExplore}            </Link>          </div></div>}        {trustEnabled && (        <div className="mt-12 grid grid-cols-2 gap-4 sm:grid-cols-4">          {trustItems.map((item, idx) => (            <div              key={idx}              className="stat-card rounded-xl px-4 py-4 text-center"            >              <div className="text-lg font-extrabold text-divine-blue text-neon-glow">{item.value}</div>              <div className="mt-0.5 text-xs text-muted">{item.label}</div>            </div>          ))}        </div>        )}      </div>    </section>  )}
