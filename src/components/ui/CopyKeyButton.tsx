'use client';import { useState } from 'react';import { Copy, Check } from 'lucide-react';interface CopyKeyButtonProps {  text: string}
export default function CopyKeyButton({ text }: CopyKeyButtonProps) {  const [copied, setCopied] = useState(false);const handleCopy = async () => {    try {      await navigator.clipboard.writeText(text);setCopied(true);setTimeout(() => setCopied(false), 2000);
}
catch {      /* Fallback; for older browsers; */ const textarea = document.createElement('textarea');      textarea.value = text;      document.body.appendChild(textarea);      textarea.select();      document.execCommand('copy');      document.body.removeChild(textarea);setCopied(true);setTimeout(() => setCopied(false), 2000)    }  };return (    <button      onClick={handleCopy}      className="flex items-center gap-1.5 rounded-md bg-primary px-3 py-2 text-sm font-medium text-primary-content transition hover:bg-primary/80"    >      {copied ? <><Check className="h-4 w-4" /> Đã copy</> : <><Copy className="h-4 w-4" /> Copy</>}    </button>  )}